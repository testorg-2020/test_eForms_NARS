name: Cloud-gov Local Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build Cloud Foundry CLI
        run: |
          echo "Hello, world"
